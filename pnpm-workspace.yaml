packages:
  - apps/signet
  - apps/signet-ui
  - apps/signet-extension
  - packages/*

# Native-build allowlist lives in the root package.json (pnpm.onlyBuiltDependencies).
# It was previously also declared here as a quoted JSON string, which pnpm does not
# parse as a list — removed to avoid a second, malformed source of truth.
