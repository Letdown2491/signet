<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Redirecting…</title>
    <style>
        body {
            margin: 0;
            min-height: 100vh;
            display: grid;
            place-items: center;
            background: radial-gradient(circle at center, #122437, #050b16 70%);
            color: #dbe6ff;
            font-family: "Inter", system-ui, sans-serif;
        }
        main {
            text-align: center;
            max-width: 420px;
            padding: 2.25rem 2rem;
            background: rgba(10, 19, 36, 0.85);
            border: 1px solid rgba(95, 126, 219, 0.4);
            border-radius: 16px;
            box-shadow: 0 28px 50px rgba(5, 12, 27, 0.5);
        }
        a, button {
            display: inline-block;
            margin-top: 1.25rem;
            padding: 0.85rem 1.25rem;
            border-radius: 12px;
            font-weight: 600;
            font-size: 1rem;
            text-decoration: none;
            background: linear-gradient(135deg, #62b4ff, #6988ff);
            color: #071125;
            border: none;
            cursor: pointer;
            box-shadow: 0 18px 35px rgba(73, 125, 255, 0.35);
            transition: transform 140ms ease;
        }
        a:hover, button:hover {
            transform: translateY(-1px);
        }
    </style>
</head>
<body>
<main>
    {{#if callbackUrl}}
        <p>You are being redirected to</p>
        <p style="word-break: break-all;"><strong>{{callbackUrl}}</strong></p>
        <a href="{{callbackUrl}}">Continue</a>
        <script>
            setTimeout(() => {
                window.location.href = "{{callbackUrl}}";
            }, 600);
        </script>
    {{else}}
        <p>Setup complete. You can now return to your client.</p>
        <button onclick="window.close()">Close this window</button>
    {{/if}}
</main>
</body>
</html>
