<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Signet Authorization</title>
    <style>
        body {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background: #0e1117;
            color: #f0f3f9;
            margin: 0;
            padding: 3rem 1rem;
            display: flex;
            justify-content: center;
        }
        main {
            width: 100%;
            max-width: 480px;
            background: rgba(20, 24, 33, 0.9);
            border: 1px solid rgba(93, 106, 133, 0.4);
            border-radius: 16px;
            padding: 2rem;
            box-shadow: 0 20px 40px rgba(4, 7, 13, 0.6);
        }
        h1 {
            margin-top: 0;
            font-size: 1.75rem;
            letter-spacing: 0.02em;
        }
        dl {
            margin: 1.5rem 0;
            border: 1px solid rgba(93, 106, 133, 0.35);
            border-radius: 12px;
            background: rgba(8, 11, 19, 0.5);
        }
        dt {
            font-weight: 600;
            font-size: 0.85rem;
            text-transform: uppercase;
            color: #8da2d4;
            padding: 1rem 1.25rem 0.35rem;
        }
        dd {
            margin: 0;
            padding: 0 1.25rem 1rem;
            word-break: break-all;
        }
        code {
            font-family: "Fira Code", "SFMono-Regular", ui-monospace, monospace;
            background: rgba(93, 106, 133, 0.2);
            padding: 0.15rem 0.35rem;
            border-radius: 6px;
        }
        form {
            display: grid;
            gap: 1rem;
        }
        label {
            display: block;
            font-size: 0.9rem;
            color: #b6c4e4;
        }
        input[type="password"] {
            width: 100%;
            border-radius: 10px;
            border: 1px solid rgba(93, 106, 133, 0.5);
            background: rgba(10, 13, 21, 0.9);
            color: inherit;
            padding: 0.75rem 1rem;
            font-size: 1rem;
        }
        input[type="password"]:focus {
            outline: none;
            border-color: #5f8bff;
            box-shadow: 0 0 0 2px rgba(95, 139, 255, 0.3);
        }
        button {
            appearance: none;
            border: none;
            border-radius: 10px;
            padding: 0.85rem 1.25rem;
            font-weight: 600;
            font-size: 1rem;
            cursor: pointer;
            transition: transform 140ms ease, box-shadow 140ms ease;
        }
        .approve {
            background: linear-gradient(135deg, #2cdd77, #23995e);
            color: #04110b;
        }
        .approve:hover {
            transform: translateY(-1px);
            box-shadow: 0 10px 25px rgba(44, 221, 119, 0.25);
        }
        .deny {
            background: rgba(93, 106, 133, 0.15);
            color: #d4deff;
        }
        .deny:hover {
            transform: translateY(-1px);
            box-shadow: 0 10px 25px rgba(93, 106, 133, 0.25);
        }
        .hint {
            font-size: 0.85rem;
            color: #8da2d4;
            text-align: center;
        }
        pre {
            background: rgba(8, 11, 19, 0.65);
            border-radius: 10px;
            padding: 1rem;
            overflow-x: auto;
            font-size: 0.85rem;
        }
    </style>
</head>
<body>
<main>
    <h1>Approve Remote Request</h1>

    <dl>
        <dt>Key</dt>
        <dd>{{record.keyName}}</dd>
        <dt>Method</dt>
        <dd><code>{{record.method}}</code></dd>
        <dt>Requester</dt>
        <dd><code>{{record.remotePubkey}}</code></dd>
    </dl>

    {{#if record.params}}
    <section>
        <h2 style="font-size:1rem;margin-bottom:0.5rem;">Payload</h2>
        <pre>{{record.params}}</pre>
    </section>
    {{/if}}

    <form method="post" action="{{urlPrefix}}/requests/{{record.id}}{{#if callbackUrl}}?callbackUrl={{callbackUrl}}{{/if}}">
        {{#unless authorised}}
        <label>
            Password
            <input type="password" name="password" autocomplete="current-password" required>
        </label>
        {{/unless}}
        {{#if callbackUrl}}
        <input type="hidden" name="callbackUrl" value="{{callbackUrl}}">
        {{/if}}
        <button type="submit" class="approve">Authorize Request</button>
        <button type="button" class="deny" onclick="window.close()">Cancel</button>
    </form>

    <p class="hint">Close this window if you prefer to deny the request.</p>
</main>
</body>
</html>
