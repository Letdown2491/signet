# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Change to use different location for dev db file
# Inside Docker the database lives at /app/config/signet.db; keep the default unless
# you know you need a different location.
DATABASE_URL="file:/app/config/signet.db"

# Add your admin Nostr npub
# ADMIN_NPUBS=npub1q2s369...

# OAuth-like web flow (optional):
# When set, the daemon exposes the Fastify web flow on the configured host/port.
# These values are merged into your signet.json on boot if present.
# AUTH_PORT=3000
# AUTH_HOST=0.0.0.0
# BASE_URL=http://localhost:3000

# React UI HTTP port (docker-compose default 4174)
# NEW_UI_PORT=4174
